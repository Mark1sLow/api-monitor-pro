# Техническое задание на проект: API Monitor Pro

## 1. Цель проекта

Создание веб-сервиса для мониторинга и анализа ключевых метрик производительности API-методов (SLI/SLO): доступность, задержка ответа, частота ошибок. Система позволяет DevOps-инженерам отслеживать ухудшения, формировать отчеты и получать уведомления по email при нарушении установленных порогов.

## 2. Роли пользователей

- **Гость**: Просмотр общих метрик (например, статус-страница), ознакомление с отчетами.
- **Зарегистрированный пользователь**: Создание и управление своими проектами и методами API, настройка SLI/SLO, просмотр детализированных графиков, получение уведомлений.
- **Администратор**: Управление пользователями, доступ к системным отчетам и логам.

## 3. Модели данных (сущности)

- **Project (Проект)**: Группировка API-методов.
  - Поля: `name`, `url`, `owner` (ForeignKey на User), `created_at`.
- **Endpoint (Эндпоинт)**: Один метод API, который мониторится.
  - Поля: `project` (ForeignKey), `method` (GET, POST и т.д.), `path`, `sla_latency_ms`, `sla_error_rate`.
- **Measurement (Измерение)**: Запись результата проверки.
  - Поля: `endpoint` (ForeignKey), `timestamp`, `response_time_ms`, `status_code`, `is_error`, `sla_breached`.

## 4. Ключевой функционал (User Stories)

- **Добавление проекта и эндпоинта**: Пользователь указывает URL проекта и методы API, которые нужно мониторить, задает SLA-параметры.
- **Просмотр графиков SLI**: На странице эндпоинта отображаются графики задержки, ошибок и доступности за последние 7 дней.
- **Уведомления о нарушениях**: При превышении лимитов (например, ошибка > 5% или задержка > 500ms) отправляется уведомление на email.
- **Генерация отчета**: Пользователь может сформировать отчет по SLI/SLO за выбранный период (PDF/HTML).
- **Автоматическая проверка**: Система по расписанию (Cron или Celery Beat) выполняет запросы к API и сохраняет результаты.

## 5. Внешние интеграции / Аналитика

- **Интеграции**: SMTP (email-уведомления), HTTP-запросы к API-методам.
- **Аналитика**: Использование **Pandas** для расчета средней задержки, количества ошибок, соблюдения SLO за период. Визуализация графиков через **Plotly** или **Matplotlib**.

## 6. Изменения в ходе реализации

### Успешно реализовано:
- ✅ Все 3 модели реализованы как запланировано (Project, Endpoint, Measurement)
- ✅ Трёхуровневая система ролей (Гость, Пользователь, Администратор)
- ✅ Автоматическая проверка API через management command `check_endpoints`
- ✅ Email уведомления при нарушении SLA
- ✅ Графики на Plotly с 3 видами визуализации (задержка, доступность, ошибки)
- ✅ Полная аналитика на Pandas (расчет SLI/SLO метрик)
- ✅ Django Admin с регистрацией всех моделей

### Отклонения от плана (обоснованные):

1. **Celery Beat → Django Management Command**
   - **Планировалось**: Использование Celery + Celery Beat для асинхронных задач
   - **Реализовано**: Django Management Command + Cron/Scheduler
   - **Причина**: Упрощение архитектуры для разработки, лучшая совместимость с PythonAnywhere, достаточно для требуемого функционала
   - **Преимущества**: Нет зависимости от Redis/RabbitMQ, проще деплоить

2. **PDF отчеты → HTML графики**
   - **Планировалось**: Генерация PDF отчетов по SLI/SLO за выбранный период
   - **Реализовано**: Интерактивные HTML графики на Plotly с выводом за последние 7 дней
   - **Причина**: Plotly графики более информативны и интерактивны, позволяют исследовать данные в реальном времени
   - **Преимущества**: Лучший UX, нет зависимостей на генерацию PDF (reportlab, wkhtmltopdf)

3. **Гостевой доступ (partial)**
   - **Планировалось**: Просмотр общих метрик гостями без регистрации
   - **Реализовано**: Требуется регистрация и вход для доступа ко всем функциям
   - **Причина**: Приоритет безопасности данных, требование изоляции между пользователями
   - **Возможность добавления**: Легко добавить публичную статус-страницу для выбранных проектов

### Дополнительно реализовано (не в исходном плане):

- ✨ **Многопользовательская изоляция данных** - Каждый пользователь видит только свои проекты
- ✨ **Полнофункциональный CRUD** - Create, Read, Update, Delete для всех сущностей
- ✨ **Адаптивный мобильный интерфейс** - Bootstrap 5 responsive design
- ✨ **Django Admin интерфейс** - Полный административный контроль с фильтрами и поиском
- ✨ **Систематическая обработка ошибок** - Try-catch блоки, валидация данных
- ✨ **Расширенная статистика на панели** - Показатели по проектам, эндпоинтам, нарушениям SLA
